/**
 * Created by anthonyrogers on 6/7/16.
 */
function LinkedList() {
    this.head = null;
    
    this.insert = function (element) {
        
    };

    this.remove = function (element) {

    };

    this.contains = function (element) {

    };

    this.size = function () {

    };
}

function Node(value) {
    this.value = value;
    this.next=null;
}

/**var newnode = newnode(value);

function addtolist(node, value) {
    if(node.next == null)
        node.next= newnode(value);
    eles;
    addtolist(node.next, value)

};*/